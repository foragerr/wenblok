import {
  container,
} from "assets/material-dashboard-react.js";

const appStyle = (theme) => ({
  content: {
    background: "#ddd",
    marginTop: "0px",
    padding: "30px 15px",
    minHeight: "100vh",
  },
  container,
});

export default appStyle;
